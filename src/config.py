
# reads the config from the 
class ConfigReader:

    # reads the file
    def read(path):
        pass

class ClfConfig:
    
    def __init__(self, token, zone):
        self.token = token
        self.zone = zone

class AppConfig:
    
    def __init__(self, tasks):
        self.tasks = tasks